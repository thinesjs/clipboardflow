import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thinesjs.clipboardflow',
  appName: 'clipboardjs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
